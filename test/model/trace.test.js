import { Coord, Matrix, Bot, State } from "../../src/model/model";
import * as trace from "../../src/model/trace";

test("Fill", () => {

  const c = new Coord(1, 0, 0)
  const fill = new trace.Fill()

});
